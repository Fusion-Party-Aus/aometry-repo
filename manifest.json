{
  "env": [
    {
      "key": "QUEUE_CHANNEL_ID",
      "description": "Discord channel snowflake for the standing #auth-queue message",
      "required": true
    },
    {
      "key": "DISCORD_GUILD_ID",
      "description": "Guild ID to register module slash commands in (omit for global commands)",
      "required": false
    },
    {
      "key": "FEDICA_API_KEY",
      "description": "Bearer token for the Fedica API — stub mode active without this",
      "required": false
    },
    {
      "key": "FEDICA_API_URL",
      "description": "Override Fedica base URL (default: https://api.fedica.com/api)",
      "required": false
    },
    {
      "key": "LLM_API_KEY",
      "description": "Anthropic API key for AI risk assessment — stub mode active without this",
      "required": false
    },
    {
      "key": "LLM_MODEL",
      "description": "Claude model ID for risk assessment (e.g. claude-sonnet-5)",
      "required": false
    },
    {
      "key": "POLICY_INDEX_URL",
      "description": "Vector store URL for policy RAG retrieval",
      "required": false
    }
  ]
}
